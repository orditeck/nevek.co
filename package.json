{
  "name": "nevek.co",
  "version": "0.1.0",
  "description": "Nevek.co Website",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "deploy": "rm -rf out && git clone git@github.com:orditeck/nevek.co.git --branch gh-pages out && npm run build && npm run export && cd out && touch .nojekyll && touch CNAME && echo \"nevek.co\" >> CNAME && cp 404/index.html 404.html && git add --all && git commit -m \"Release at $(date)\" && git push && cd .. && rm -rf out",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Keven Lefebvre <keven@nevek.co>",
  "license": "MIT",
  "dependencies": {
    "@zeit/next-sass": "^1.0.2-canary.0",
    "classnames": "^2.2.6",
    "next": "^7.0.2",
    "next-compose-plugins": "^2.1.1",
    "next-manifest": "^1.6.0",
    "next-offline": "^3.0.4",
    "node-sass": "^4.9.4",
    "normalize.css": "^8.0.0",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "swiper": "^4.4.1"
  }
}
